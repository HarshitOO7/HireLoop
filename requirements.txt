# AI providers
anthropic
openai
google-generativeai
groq
httpx

# Telegram bot
python-telegram-bot[job-queue]

# Job scraping
python-jobspy
# httpx already listed above (used by Jina Reader + Adzuna API too)

# Adzuna free job API client (optional — only needed if ADZUNA_APP_ID is set)
# Register free at https://developer.adzuna.com
# pip install httpx  ← already listed above, that's all Adzuna needs

# Database
sqlalchemy[asyncio]
aiosqlite
alembic

# Config
python-dotenv

# PDF export (pure Python — no system dependencies)
reportlab

# Background scheduling (embedded in bot process)
apscheduler

# Resume parsing (input)
pypdf2
python-docx

# Markdown processing
markdown
